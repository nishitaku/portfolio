---
const { name, tooltip } = Astro.props;
import { Icon } from 'astro-icon/components';
---

<div
  class="tooltip bg-base h-8 w-8 place-content-center place-items-center rounded-full shadow-sm"
  data-tip={tooltip}
>
  <Icon name={name} size="24" />
</div>
